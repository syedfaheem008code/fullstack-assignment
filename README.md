# Fullstack Assignment — GitHub Submission Ready

## Structure
- **backend/** → Express + MongoDB + JWT + seed + validation
- **frontend/** → React + TypeScript (Vite)
- **meeting-room-conflict/** → Pure TypeScript + Jest
- **README.md** → Full instructions

## How to Run
### Backend
```bash
cd backend
npm install
cp .env.example .env
npm run seed
npm run dev
```
### Frontend
```bash
cd frontend
npm install
npm run dev
```
### Utility
```bash
cd meeting-room-conflict
npm install
npm test
```
